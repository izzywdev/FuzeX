---
name: mobile-packaging
description: Use when packaging a web app as a mobile app (PWA + Capacitor → .apk/.ipa).
---

# mobile-packaging

Covers: web app manifest + service worker + offline; Capacitor shell + native plugins (camera/storage/biometrics/deep-links); icons/splash; signing the artifact; PWA↔native parity. Owned by mobile-app-engineer.

*(Structured stub — flesh out with concrete commands/checks as the first repo adopts it.)*
