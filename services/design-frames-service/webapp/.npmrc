# GitHub Packages registry mapping for the private FuzeFront design system.
# CI supplies GITHUB_TOKEN (read:packages); do NOT commit a literal token here.
@izzywdev:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
